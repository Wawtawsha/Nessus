---
phase: 07-final-polish-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nspc-valentines/components/layouts/split-decision.tsx
  - nspc-valentines/app/globals.css
autonomous: true

must_haves:
  truths:
    - "User perceives consistent vertical spacing scrolling through the page"
    - "User on mobile finds all interactive elements easy to tap (min 44px touch targets)"
    - "User experiences subtle micro-interactions on hover, focus, and click"
    - "User sees cohesive color harmony across all page sections"
  artifacts:
    - path: "nspc-valentines/components/layouts/split-decision.tsx"
      provides: "Polished layout with consistent spacing, mobile optimization, micro-interactions"
    - path: "nspc-valentines/app/globals.css"
      provides: "Any new utility classes for focus rings or micro-interaction keyframes"
  key_links:
    - from: "split-decision.tsx"
      to: "globals.css"
      via: "Tailwind utility classes and custom CSS"
      pattern: "animate-|focus-visible"
---

<objective>
Final polish pass on the NSPC Valentine's Day landing page: normalize vertical rhythm, optimize mobile touch targets, add micro-interactions to all interactive elements, and verify color harmony.

Purpose: Ship a polished, professional landing page that feels intentional at every scroll position and device size.
Output: Updated split-decision.tsx and globals.css with all four POLISH requirements addressed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@nspc-valentines/components/layouts/split-decision.tsx
@nspc-valentines/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vertical rhythm normalization + mobile optimization (POLISH-01, POLISH-02)</name>
  <files>nspc-valentines/components/layouts/split-decision.tsx</files>
  <action>
  **Vertical Rhythm (POLISH-01):**
  Standardize section spacing to a consistent scale. Current state is inconsistent: hero `py-10`, comparison `py-12`, fallback `py-8`, divider `py-3`.

  Apply this spacing system:
  - Hero halves: `py-12 px-6` (bump from py-10 to match comparison)
  - Divider: `py-4` (bump from py-3 for breathing room)
  - Expanded menu section: keep animated `py-8`/`py-0` but add consistent outer wrapper
  - Comparison section: `py-12 px-4` (keep as-is, this is the baseline)
  - "Still deciding?" fallback: `py-12 px-4` (bump from py-8 to match comparison)
  - Sticky bar: keep `py-3` (different context, fixed element)

  **Mobile Optimization (POLISH-02):**
  - Hero heading: change `text-3xl md:text-4xl lg:text-5xl` to `text-2xl sm:text-3xl md:text-4xl lg:text-5xl` (prevent overflow on 320px screens)
  - "See full menu" / "Hide menu" buttons: add `min-h-[44px] min-w-[44px] inline-flex items-center justify-center px-4` for WCAG touch target compliance. Remove bare `underline`, use `underline underline-offset-4` for better legibility.
  - Comparison cards: add `active:scale-[0.98]` for mobile tap feedback since hover doesn't exist on touch.
  - Sticky bar button: verify it already meets 44px min-height (MetalButton likely does, but add `min-h-[48px]` to the wrapper if needed for comfortable tapping).
  - Comparison card click area: already full-card, good. Add `select-none` to prevent text selection on tap.
  </action>
  <verify>
  Run `npx next build` from nspc-valentines directory to confirm no build errors. Visually inspect: open dev tools at 320px, 375px, 768px, 1024px widths and confirm spacing feels consistent and touch targets are at least 44px.
  </verify>
  <done>All section py values follow consistent py-12 scale (except sticky bar). All interactive elements meet 44px minimum touch target. Hero text does not overflow on 320px screens.</done>
</task>

<task type="auto">
  <name>Task 2: Micro-interactions + color harmony (POLISH-03, POLISH-04)</name>
  <files>nspc-valentines/components/layouts/split-decision.tsx, nspc-valentines/app/globals.css</files>
  <action>
  **Micro-interactions (POLISH-03):**

  Hero halves — add subtle hover effect on desktop:
  - Each hero half: add `md:hover:brightness-110 transition-[filter] duration-300` to the outer div. This brightens the background on hover without changing layout.

  "See full menu" buttons — enhance interaction:
  - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-valentine-gold focus-visible:ring-offset-2 focus-visible:ring-offset-valentine-red rounded-sm` (left hero) and `ring-offset-valentine-red-light` (right hero).

  Comparison cards — add focus states:
  - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-valentine-gold focus-visible:ring-offset-2` to each card div.
  - Add `tabIndex={0}` and `role="button"` for keyboard accessibility.
  - Add `onKeyDown` handler: Enter/Space triggers the same onClick.

  "Contact Us" button and "Reserve" buttons — these use MetalButton which should already handle focus. Verify and skip if already handled.

  Date badge — add a subtle float animation:
  - In globals.css, add a `@keyframes float` that translates Y by 4px over 3s with ease-in-out, infinite. Apply as `.animate-float` utility.
  - Apply `animate-float` to the date badge span (replace or supplement existing animate-in after entrance completes).

  **Color Harmony (POLISH-04):**
  Audit and fix:
  - Expanded menu bg-valentine-pink-light into comparison bg-valentine-cream: these are close but distinct. Add a thin top border to comparison section: `border-t border-valentine-gold/20` to create a clean visual break.
  - Fallback "Still deciding?" section: currently bg-valentine-pink-light same as expanded menu. Change to `bg-valentine-cream/50` (half-opacity cream) to differentiate from expanded menu while staying harmonious. Or keep bg-valentine-pink-light but add `border-t border-valentine-gold/20` for visual separation.
  - Divider text: currently `text-valentine-red`. This is fine — it anchors to the hero color. Keep.
  - Sticky bar: bg-valentine-red matches left hero. Good continuity. Keep.

  Choose the border-t approach for section separation — minimal change, clear visual breaks.
  </action>
  <verify>
  Run `npx next build` from nspc-valentines directory. Check: hover over hero halves shows brightness change on desktop. Tab through page — all interactive elements show gold focus ring. Date badge floats gently. Section transitions have subtle gold borders separating them.
  </verify>
  <done>All interactive elements have hover, focus-visible, and active states. Date badge has float animation. Sections are visually separated with consistent border treatments. No color clashes between adjacent sections.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All section vertical padding follows py-12 baseline (except sticky bar and divider)
- All buttons/interactive elements meet 44px touch target minimum
- Tab navigation shows focus rings on all interactive elements
- Hero halves respond to hover on desktop
- Adjacent sections have clear visual separation
- No jarring color transitions scrolling top to bottom
</verification>

<success_criteria>
1. Consistent vertical rhythm: all major sections use py-12, creating even breathing room
2. Mobile optimization: hero text fits 320px, all touch targets >= 44px, tap feedback on cards
3. Micro-interactions: hero hover brightness, focus-visible rings, date badge float, card active:scale
4. Color harmony: border-t separators between sections, no adjacent same-background clashes
5. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-final-polish-pass/07-01-SUMMARY.md`
</output>
